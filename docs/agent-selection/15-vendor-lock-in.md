---
title: 供应商锁定怎么评估
description: 从模型、工具协议、状态、Trace、评测集、数据保留和迁移路径评估 Agent 平台锁定风险。
contentType: support
series: support
contentId: agent-selection-vendor-lock-in
shortTitle: 供应商锁定评估
summary: provider 锁定不是不能接受，但必须知道被锁在哪里、退出成本是什么。
difficulty: advanced
estimatedTime: 25 分钟
learningGoals:
  - 识别 Agent 平台锁定发生在哪些层
  - 判断哪些锁定可以接受，哪些会伤害生产治理
  - 设计可迁移的状态、工具、评测和观测边界
prerequisites:
  - 已了解 Build vs Buy
  - 已了解 Agent 选型评审模板
recommendedNext:
  - /agent-selection/17-sdk-tools-langgraph
  - /agent-selection/18-managed-platform-vs-runtime
  - /practice/p20-observability/
practiceLinks:
  - /practice/p18-model-routing/
  - /practice/p20-observability/
  - /practice/p21-evaluation/
searchTags:
  - 供应商锁定
  - provider lock-in
  - 托管 Agent
  - 平台迁移
navigationLabel: 供应商锁定评估
entryMode: bridge
roleDescription: 适合采购平台、选择托管 Agent 或评估迁移风险时阅读。
---

<ChapterLearningGuide />

## 锁定不是一个点

Agent 平台锁定通常发生在多层：

```text
Model
  -> Tool Schema
  -> Agent Runtime
  -> State
  -> Trace
  -> Eval
  -> Data Policy
```

只看模型能不能替换是不够的。真正难迁移的通常是状态、工具、trace 和评测。

## 锁定分两类

| 类型 | 含义 | 处理 |
| --- | --- | --- |
| 可接受锁定 | 换掉会麻烦，但不伤业务核心 | 写清楚成本即可 |
| 不可接受锁定 | 锁住权限、状态、审计、评测 | 不建议上线 |

例如模型 provider 锁定可能可接受，但审计日志不可导出通常不可接受。

## 锁定风险表

| 层 | 风险 | 可接受条件 |
| --- | --- | --- |
| 模型 | 提示词和输出格式绑定 | 有模型路由和回归评测 |
| 工具协议 | 工具 schema 不能复用 | 工具定义可导出 |
| Runtime | 状态和流程绑定平台 | 业务状态可外置 |
| Trace | 只能在平台内看日志 | trace 可导出或回放 |
| Eval | 评测集只存在平台内 | 样本和评分标准自有 |
| 数据 | 保留、删除策略不透明 | 合规条款明确 |

## 先区分可替换和可治理

很多团队只问“能不能换模型”，但生产 Agent 更重要的是“换了之后能不能证明没有变差”。

| 能力 | 可替换 | 可治理 |
| --- | --- | --- |
| 模型 | 能切到另一个 provider | 有评测集、回归样本和输出差异报告 |
| 工具 | 能改成另一个 schema | 有权限、审计和参数校验 |
| Trace | 能下载日志 | 能回放关键案例 |
| RAG | 能重建索引 | 保存原文、chunk、metadata 和版本 |
| Runtime | 能重新实现流程 | 状态机和业务状态由自己掌握 |

只可替换但不可治理，迁移后很难判断系统是否仍然安全、准确和可审计。

## 迁移演练

评估平台时可以做一个小演练：

```text
导出 20 条典型 trace
导出工具 schema
导出或复现 prompt / policy
用另一套 runtime 重放 5 条样本
对比输出、工具调用和引用
```

如果这个演练做不到，说明你并不知道退出成本。

## 迁移时最容易漏掉什么

真正迁移时，最容易漏掉的不是模型调用代码，而是围绕它的工程资产：

- 用户反馈；
- 失败样本；
- prompt 版本；
- 工具调用历史；
- 权限判断记录；
- 引用和来源；
- 人工审批结果；
- 成本和延迟指标。

这些资产决定系统能不能持续改进。平台如果只允许你导出最终答案，不允许导出过程数据，就会把优化能力也锁住。

## 必问问题

评估供应商时，至少问：

- Prompt 和工具定义能否导出；
- trace 能否批量导出；
- 状态 schema 是否由你控制；
- 是否能替换模型；
- 是否能接入自己的权限系统；
- 是否能接入自己的评测集；
- 数据删除和保留策略是否清楚；
- 迁出后是否能继续回放历史案例。

如果对方只能回答“平台内支持”，就要把它视为锁定成本。

## 采购评审怎么写

锁定风险不应该只写一句“存在供应商依赖”。评审材料至少要写清：

| 问题 | 示例回答 |
| --- | --- |
| 锁在哪里 | 模型、trace、runtime 状态 |
| 为什么接受 | 第一阶段只做低风险内部问答 |
| 退出触发条件 | 成本超过预算、合规要求变化、质量下降 |
| 退出动作 | 导出 trace、重放样本、切换模型路由 |
| 最坏后果 | 短期无法回放历史会话 |

这样评审才知道风险是被接受了，还是被忽略了。

## 哪些锁定可以接受

可以接受的锁定：

- 非核心原型；
- 明确短期项目；
- 标准化场景；
- 供应商能力明显领先；
- 退出路径已经写清楚。

不建议接受的锁定：

- 权限系统绑定；
- 业务状态不可导出；
- 审计日志不可导出；
- 评测集不能迁移；
- 数据删除策略不清；
- 高风险工具只能在黑盒里执行。

## 降低锁定的方法

- 把业务状态放在自有数据库；
- 工具 schema 使用平台无关定义；
- 保留自己的评测集；
- 保存关键 trace；
- 模型调用加路由层；
- RAG 数据和索引构建流程自有；
- 高风险工具审批放在自有系统。

你不需要把所有东西都自研，但关键边界必须握在自己手里。

## 降低锁定的最小做法

第一版不一定要做完整平台抽象，但至少可以做三件小事：

1. 保存自有评测样本和期望输出；
2. 保存工具 schema、参数和执行结果；
3. 保存关键 trace 的平台无关摘要。

这三件事成本不高，却能让后续模型替换、供应商评估和事故复盘有依据。没有这些记录，迁移只能靠感觉。

## 最终判断

```text
能导出：可评估
能替换：可优化
能回放：可治理
能迁移：可接受
不能解释退出路径：高风险锁定
```

供应商锁定不是采购部门的问题，而是系统能不能长期演进的问题。
